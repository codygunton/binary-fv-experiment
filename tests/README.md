# Target-independent tests

This directory contains tests for reusable specifications and infrastructure. Tests that compare or
mutate one concrete binary remain with that binary under `targets/`.

`Specs/SSZ/` exercises the Amsterdam V4 executable specification independently of Zesu.
