# SSZ specification tests

`AmsterdamV4Test.lean` exercises the project-owned Amsterdam V4 schema, canonical-wire checks, outer
framing, and complete raw-value projection independently of any target binary.

Run it from `tools/ssz-oracle` with `lake exe ssz_oracle_test`.
