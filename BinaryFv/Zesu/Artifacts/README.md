# Artifacts

`Image.lean` binds the generated production ELF load image to the generated Level 1 manifest digest.
These are immutable facts about the exact binary used by evidence capture and machine proofs.
