import BinaryFv.Keccak.Reth.Proof.Helpers.Memset.Contract

/-!
# `memset`

Umbrella for the `memset` function contract. The proof is split into byte store, framing,
definitions, per-instruction steps, the loop, and the capstone contract.
-/
