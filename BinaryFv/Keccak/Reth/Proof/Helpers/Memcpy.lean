import BinaryFv.Keccak.Reth.Proof.Helpers.Memcpy.Contract

/-!
# `memcpy`

Umbrella for the `memcpy` function contract, split into register reductions, framing, the byte
move, the loop invariant, the loop, and the capstone contract.
-/
