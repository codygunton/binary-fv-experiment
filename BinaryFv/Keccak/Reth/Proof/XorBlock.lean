import BinaryFv.Keccak.Reth.Proof.XorBlock.SpecCorrespondence

/-!
# `xor_block`

Umbrella for the `xor_block` absorb-step contract: execute/register reductions, byte assembly,
framing, the three families of body steps, the store's memory effect, the loop invariant, the body
core, the loop, the capstone contract, and the correspondence with `Spec.Keccak`.
-/
